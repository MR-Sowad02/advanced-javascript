const students = [
     {id: 21, name: "uuyiir"},
     {id: 31, name: "hshfhie"},
     {id: 41, name: "dhghe"},
     {id: 32, name: "ghtte"}
];

const names = students.map(s => s.name);

const id = students.map(s => s.id);

console.log(names);
console.log(id);